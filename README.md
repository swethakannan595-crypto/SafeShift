# SafeShift
SafeShift is a browser-based, multi-modal worker safety monitor that uses an 8-class Teachable Machine image classifier running on TensorFlow.js to detect helmet, mask, ID badge, and posture compliance from a live camera feed. It runs entirely client-side — no backend required — with real-time severity-tiered alerts and CSV-exportable incident log.
